import _ from 'underscore';
import {View} from 'backbone.marionette';

const helloTemplate = _.template(`
  <h1>Hello <%%= name %>!</h1>
`);

export const Hello = View.extend({
  template: helloTemplate,
  el: '#root'
});
