import $ from 'jquery';
import {Model} from 'backbone';

import {Hello} from './app/hello';

import './index.<%- css %>';

const world = new Model({name: 'world'});

const helloView = new Hello({model: world});

$('#root').append(helloView.render().el);
